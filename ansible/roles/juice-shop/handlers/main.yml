---
# Handlers for role: juice-shop
# ==============================================================================

- name: Restart juice-shop
  ansible.builtin.command: docker compose -f {{ juice_shop_install_dir }}/docker-compose.yml restart
  changed_when: true

- name: Restart wazuh-agent
  ansible.builtin.service:
    name: wazuh-agent
    state: restarted
