# SOCForge — Atomic Red Team Operator Environment
export ATOMIC_RED_TEAM_PATH="{{ atomic_red_team_path }}"
export INVOKE_ATOMIC_PATH="{{ invoke_atomic_path }}"
export SOCFORGE_ATOMIC_CATALOG="{{ atomic_catalog_path }}"
export SOCFORGE_ATOMIC_LOG="{{ atomic_log_file }}"
export SOCFORGE_TARGET_WINDOWS="{{ atomic_target_host }}"
